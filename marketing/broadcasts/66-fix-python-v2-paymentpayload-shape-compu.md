# Broadcast — PR #66 — fix(python): v2 PaymentPayload shape + compute-budget + memo (x402 handshake)

**Type**: drafted by ship-broadcast agent v0.1.0
**Linked PR**: https://github.com/vdmnexus/vdmnexus/pull/66
**Drafted**: 2026-05-23 10:43 UTC by ship-broadcast agent (model: anthropic/claude-sonnet-4)
**Status**: in review — human approval gate required before scheduling

---

## X (248/280)

```
Fixed two bugs in the Python SDK's x402 handshake caught by smoke test.

vdm-nexus 0.2.1 is live on PyPI — the PaymentPayload now matches v2 spec shape and includes compute-budget + memo instructions that the facilitator expects.

docs.vdmnexus.com
```

## Farcaster (243/320)

```
Fixed two bugs in the Python SDK's x402 handshake caught by smoke test.

vdm-nexus 0.2.1 is live on PyPI — the PaymentPayload now matches v2 spec shape and includes compute-budget + memo instructions that the facilitator expects.

vdmnexus.com
```

## Telegram (592/4096)

```
**Python SDK fix: x402 handshake now works on mainnet**

vdm-nexus 0.2.1 is live on PyPI. Fixed two bugs caught by smoke test against nexus.vdmnexus.com:

**Bug #1**: PaymentPayload was v1-shaped but tagged as v2, causing "Cannot read properties of undefined (reading 'scheme')" from @x402/core dispatcher

**Bug #2**: Missing compute-budget and memo instructions that the facilitator verifier expects

The Python SDK now matches @x402/svm exactly — SetComputeUnitLimit + SetComputeUnitPrice + TransferChecked + Memo in the correct order.

`pip install --upgrade vdm-nexus`

docs.vdmnexus.com
```

## LinkedIn (1053/3000)

```
When you're building infrastructure that settles payments on-chain, wire protocol compatibility is not negotiable. A single field mismatch or missing instruction can break the entire payment flow.

Caught two bugs in the Python SDK's x402 handshake during smoke testing against nexus.vdmnexus.com:

— PaymentPayload was v1-shaped but tagged as x402Version: 2, causing the @x402/core dispatcher to error on "Cannot read properties of undefined (reading 'scheme')"
— Missing SetComputeUnitLimit, SetComputeUnitPrice, and Memo instructions that the facilitator verifier expects in specific positions

vdm-nexus 0.2.1 is live on PyPI. The Python SDK now matches @x402/svm exactly: four instructions in the correct order, v2 PaymentPayload shape with the accepted requirements entry, proper compute budget allocation.

This is why we smoke test against live mainnet infrastructure before calling anything "shipped." The autonomous agent payment rail has to work on the first call — there's no human in the loop to retry a failed handshake.

docs.vdmnexus.com
```

## Visual

- File: `marketing/media/out/python-x402-fix-code.png`
- Source: `silicon`
- Rationale: Ship is a wire protocol bug fix — the corrected PaymentPayload construction and instruction ordering are the actual news. Code shape shows what changed better than a demo.
- Input file: `packages/sdk-python/src/vdm_nexus/x402.py`

---

## Schedule

| Platform | Status | Scheduled for | Postiz ID |
|---|---|---|---|
| X | pending review | — | — |
| Farcaster | pending review | — | — |
| Telegram | pending review | — | — |
| LinkedIn | pending review | — | — |

---

## Agent provenance

- Agent: ship-broadcast v0.1.0
- Model: `anthropic/claude-sonnet-4`
- Receipt: [`vdmnexus.com/r/895d38a9-19e3-4d7d-9adc-0f352e3f0769`](https://vdmnexus.com/r/895d38a9-19e3-4d7d-9adc-0f352e3f0769)
- Settlement tx: `5PhCR5dkdbzhXoiF2idBLnwi4SL3WDU7Miqg19VgMoGSpTFTYNNycHViB86Eg9wPqLfQw2qN5LVPUhFQHE25mRH1`

_Generated by the Nexus ship-broadcast agent. Drafts only — no platform was contacted. Approve per-platform before scheduling._
