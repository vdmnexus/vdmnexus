# Broadcast — PR #65 — feat(python): @vdm-nexus/sdk-python v0.2.0 (x402) + langchain-vdm-nexus

**Type**: drafted by ship-broadcast agent v0.1.0
**Linked PR**: https://github.com/vdmnexus/vdmnexus/pull/65
**Drafted**: 2026-05-23 10:37 UTC by ship-broadcast agent (model: anthropic/claude-sonnet-4)
**Status**: in review — human approval gate required before scheduling

---

## X (276/280)

```
Python SDK v0.2.0 + langchain-vdm-nexus are live on PyPI.

pip install langchain-vdm-nexus → LangChain, LangGraph, and CrewAI agents now pay per inference in USDC on Solana mainnet and get signed inference receipts. Every major Python framework just opened.

docs.vdmnexus.com
```

## Farcaster (271/320)

```
Python SDK v0.2.0 + langchain-vdm-nexus are live on PyPI.

pip install langchain-vdm-nexus → LangChain, LangGraph, and CrewAI agents now pay per inference in USDC on Solana mainnet and get signed inference receipts. Every major Python framework just opened.

vdmnexus.com
```

## Telegram (765/4096)

```
**Python framework ecosystem unlocked for signed inference**

Two packages just shipped to PyPI:

• **vdm-nexus 0.2.0** — adds X402Agent for pay-per-call inference on Solana mainnet
• **langchain-vdm-nexus 0.1.0** — ChatNexus wraps the x402 handshake in LangChain's BaseChatModel

Result: LangChain, LangGraph, and CrewAI agents can now authenticate with their own Ed25519 keypair, pay per inference call in USDC via x402, and receive cryptographically verifiable signed inference receipts.

```python
from langchain_vdm_nexus import ChatNexus

model = ChatNexus(
    nexus_url="https://nexus.vdmnexus.com/api/v1",
    model="openai/gpt-4o-mini"
)
response = model.invoke("hello world")
# receipt available in response.response_metadata
```

Docs: docs.vdmnexus.com
```

## LinkedIn (1256/3000)

```
Python is the substrate for most enterprise AI agent development. LangChain powers autonomous systems at regulated institutions. CrewAI orchestrates multi-agent workflows. LangGraph builds complex reasoning chains.

As of today, every Python agent framework can authenticate, pay for, and produce cryptographically verifiable inference on Solana mainnet.

Two packages shipped to PyPI this morning:

vdm-nexus 0.2.0 adds X402Agent — Python agents hold their own Ed25519 keypair, pay per inference call in USDC via x402 on Solana mainnet, and receive signed inference receipts on every successful call.

langchain-vdm-nexus 0.1.0 adds ChatNexus — a LangChain BaseChatModel that wraps the x402 handshake. This single adapter unlocks LangChain, LangGraph, and CrewAI with no framework-specific plumbing.

The use cases this opens:

— Regulated industries can deploy autonomous agents with cryptographic audit trails for every model interaction
— Multi-agent systems where each agent operates on its own budget, with verifiable spend and output
— Enterprise workflows where AI reasoning chains are independently auditable by compliance teams

Every major Python agent framework now works with one line: pip install langchain-vdm-nexus.

Docs: docs.vdmnexus.com
```

## Visual

- File: `marketing/media/out/langchain-chat-nexus.gif`
- Source: `vhs:langchain-chat-nexus`
- Rationale: Existing tape matches the ship perfectly — pip install + ChatNexus demo showing the LangChain integration.
- Input file: `marketing/media/vhs/langchain-chat-nexus.tape`

---

## Schedule

| Platform | Status | Scheduled for | Postiz ID |
|---|---|---|---|
| X | pending review | — | — |
| Farcaster | pending review | — | — |
| Telegram | pending review | — | — |
| LinkedIn | pending review | — | — |

---

## Agent provenance

- Agent: ship-broadcast v0.1.0
- Model: `anthropic/claude-sonnet-4`
- Receipt: [`vdmnexus.com/r/2639c835-1dd3-47a1-84aa-4cb2e3c6ef11`](https://vdmnexus.com/r/2639c835-1dd3-47a1-84aa-4cb2e3c6ef11)
- Settlement tx: `4WyftaNqBbbsdiXD4RMShoRq8JzHXLJeCrFBm3GCGQEkTiE49we9rkzNzF7RLbZozn9XX6M4VfVF6QX2pPws7r6y`

_Generated by the Nexus ship-broadcast agent. Drafts only — no platform was contacted. Approve per-platform before scheduling._
