# Daily review — 2026-09-13

## 0. Slack feedback check

Read `#nexus` back through the last checkpoint (2026-09-12 22:11 CEST).
All 66 messages visible in channel history are this loop's own automated
daily-review posts under the Dennis Slack identity — zero human replies
anywhere in the channel. Nothing to apply.

## 1-2. Gather + review

- **PRs/commits since last run:** none. `main` is still at the #199
  merge commit (`2828504`, 2026-09-12T20:11:25Z). No new PRs opened or
  merged, no commits pushed outside this loop's own planning cadence.
- **#199 (2026-09-12 daily review)** — confirmed merged cleanly the
  same session (per its own body: 7 seconds open-to-merge). **Eighth
  consecutive clean night** under the corrected merge logic (#192-#199).
  The fix stays proven; no action needed here.
- **Open PRs, re-verified fresh:**
  - #106 (cards-v1 spec) — still open, unchanged, **112 days**. Still a
    merge-or-close decision for Dennis, not a build task.
  - #95 (Polymarket agent, Phase A) — still open, unchanged, **113
    days**. Still blocked on Spanish counsel.
- **Kill-criteria check** (`CLAUDE.md` + `marketing/token-launch-checklist.md`):
  nothing new trips a kill criterion tonight. The live deposit-crediting
  bug below is a running concern, not a fresh trigger.

### Health checks (read-only)

All three hosts failed with `CONNECT tunnel failed, response 403` from
this session's own egress proxy, and the `/api/playground/inference`
POST failed the same way — **45th consecutive confirmed blocked night**
on `nexus.vdmnexus.com/api/health`, `verify.vdmnexus.com`, and
`www.vdmnexus.com`. Unchanged root cause (session egress policy, not a
product outage) — still needs a decision from Dennis: allowlist these
hosts, or move the checks to run from somewhere else.

### Deposit-crediting bug — reconfirmed fresh against live production logs

Pulled `apps/nexus` runtime logs directly from Vercel for the production
deployment (`dpl_kMXumydsSvke6TjRGXWJZXbdqA3g`, still serving — no new
`nexus` deployment since 2026-08-19T20:13Z, unchanged). Every
`/api/v1/deposits/scan` invocation in the last two hours
(2026-09-13T18:xx-20:08 UTC, ~2-minute cadence) still hits
`RPC getTransaction HTTP 429` on the scanned signatures and completes
with `credited: 0` (`txs_seen: 10-12, skipped: 9-10` each run — 85
occurrences of "429" in the pulled log window). **23 days running**
(since 2026-08-21). Any on-chain USDC deposit landing in this window is
very likely still not being credited. This needs Dennis's decision on
the RPC provider/rate limit — it is not something another nightly
re-check will resolve.

### `nexus` Vercel project — reconfirmed fresh

`list_teams` confirms `vdm-nexus` is still on the Hobby plan.
`list_deployments` for the `nexus` project shows no deployment newer
than the 2026-08-19T20:13Z one still serving production — **still
stuck, unchanged root cause** (same one blocking new deploys since
08-19; separately, this is very likely also why the deposit-scan cron
keeps hitting the same rate-limited RPC path without a code fix ever
landing). Both process-integrity asks below remain open.

### Rienda M1-M5

No report from Dennis since 2026-07-31 (spec complete; token + Uniswap
v4 fee-burn hook contracts, 26 passing tests; M1 in development).
**44 days stale.**

### Launch readiness (`marketing/token-launch-checklist.md`)

No movement. The T-14 / T-48h / T-0 steps still describe Solana tooling
(pump.fun, Squads, Solscan, Bubblemaps Solana) and still need a rewrite
pass for the Uniswap v4 / Robinhood Chain venue — **45 days unpicked**
since the 2026-07-30 re-venue.

## 3-4. Plan tomorrow + persist

See `planning/NEXT.md` (overwritten alongside this file) for the full
ordered plan. Two asks stay open and unanswered in `#nexus`: the
`nexus` Vercel project's failing check (23 days) and auto-merge for
planning PRs (35 days).

`STATUS.md` updated with tonight's clean-merge confirmation for #199
and this session's re-verification of the deposit bug / Vercel check.

full plan: `planning/daily/2026-09-13.md`
