# Daily review — 2026-08-21

## 0. Slack feedback applied

Read `#nexus` from 2026-08-19T00:00 UTC forward (covers the window
since the last merged checkpoint, planning/daily/2026-08-18.md). Two
messages in the window, both this loop's own automated nightly
summaries (2026-08-19 22:14 CEST, 2026-08-20 22:18 CEST) — nothing
from Dennis. Nothing actioned in step 0.

## 1. Gather

- **`planning/daily/` on `main` is stale — last file is 2026-08-18.**
  Two more nightly reviews ran and pushed to their own branches since
  then but neither merged: PR #175 (`claude/nifty-cray-a98ov8`, 2026-08-19
  review) and PR #176 (`claude/nifty-cray-pnokp3`, 2026-08-20 review) are
  both still open. Read both directly off their branches for continuity.
- **Root cause of the "nexus" deploy failures, found and confirmed
  tonight.** #176 flagged that the `Vercel – nexus` check had started
  failing identically on unrelated commits and guessed a stalled
  pipeline. Investigated further this session via the Vercel API:
  - The actual error posted by the Vercel bot on #176 is explicit:
    *"Hobby accounts are limited to daily cron jobs. This cron
    expression (`*/2 * * * *`) would run more than once per day.
    Upgrade to the Pro plan to unlock all Cron Jobs features on
    Vercel."* — `apps/nexus/vercel.json` has always run the
    deposit-scan cron at `*/2 * * * *` (every 2 minutes, per
    `CLAUDE.md`); this only started blocking builds after
    2026-08-18T20:13Z, consistent with the `vdm-nexus` Vercel team
    dropping to (or already being on) the Hobby tier, which caps cron
    frequency to once/day. No tool available here exposes a raw
    plan/billing field to confirm the tier directly, so this is
    inferred from the error text and timing, not confirmed by a
    billing API.
  - Confirmed via `list_deployments`: **zero** deployment records of
    any kind (success or failure) exist for the `nexus` project after
    2026-08-18T20:13:09Z (`dpl_87VBtuekpVTR6Xqjc8RMZ6WQT3aw`, the last
    one that built). Both #175 and #176 pushes never got a deployment
    object created at all — the plan/cron check fails before build,
    matching #176's own observation.
  - Sibling projects on the same team (`vdmnexus-web`, `docs`,
    `console`) deployed successfully on both #175 and #176 — this is
    isolated to the `nexus` project's cron config, not an account-wide
    outage.
  - Production `nexus.vdmnexus.com` is unaffected and still serving
    `dpl_kMXumydsSvke6TjRGXWJZXbdqA3g` (PR #173 / commit `f0b53cf`,
    ready since 2026-08-18T20:09:38Z). Vercel does not retroactively
    kill a cron already attached to a live deployment when the plan
    drops below the tier that allows it — it only blocks *new*
    deployments that carry a violating cron config. So the existing
    cron kept firing.
- **New, separate finding: the deposit-scan cron is running but not
  crediting anything.** `get_runtime_logs` on the live production
  deployment shows `GET /api/v1/deposits/scan` firing every ~2 minutes
  continuously through 2026-08-21T20:12:23Z (today), but each run is
  hitting Solana RPC `getTransaction` calls that return **HTTP 429**
  (rate-limited) and crediting **0 of ~10** transactions per run. This
  is independent of the deploy-blocker above — it means on-chain USDC
  deposits from agents are very likely not being credited to the
  ledger right now, even though the cron itself is alive. Nothing was
  changed in response — read-only finding, flagging for Dennis.
- Per the standing "never merge without confirmed-green CI" policy
  this loop has followed since the #142-#174 stuck-PR pattern:
  **not merging #175 or #176 tonight.** The `Vercel – nexus` check is
  genuinely red on both (not a false-stuck-despite-green case), even
  though the cause is now understood to be unrelated to the docs-only
  diffs in either PR. Tonight's own commit will almost certainly hit
  the same failing check for the same reason (`apps/nexus/vercel.json`
  is unchanged), so this is likely to become a 3-PR backlog
  (#175, #176, and tonight's) unless the cron/plan issue is resolved
  or the merge policy for planning-only PRs changes — see plan below.
- No product code merged since #162 (2026-08-06) — 15 days now.
- Open PRs unchanged: #106 (cards-v1 spec, 89 days) and #95
  (Polymarket, blocked on Spanish counsel, 90 days).
- `STATUS.md`: read, updated below.
- `marketing/token-launch-checklist.md`: unchanged since the
  2026-07-30 re-venue log entry.
- `CLAUDE.md` + launch-checklist kill-criteria: none tripped.
- `business-plan.md` still does not exist in `vdmnexus/internal` — not
  creating it without an explicit go-ahead from Dennis.
- Health checks (read-only, one call each): all three still
  egress-blocked — `curl: (56) CONNECT tunnel failed` on
  `www.vdmnexus.com/api/playground/inference`, `verify.vdmnexus.com`,
  and `nexus.vdmnexus.com/api/health`. At least the 22nd consecutive
  confirmed night since 2026-07-31 (2026-08-13 remains an unconfirmed
  gap in the streak, not a resolution). Reporting as "not run."

## 2. Review

- No product code has shipped since #162 (2026-08-06) — 15 days of
  quiet code, now compounded by three consecutive nights of the
  planning loop's own PRs also not landing (#175, #176, and tonight's),
  for a newly-diagnosed and different reason than the earlier
  green-CI-but-unmerged pattern.
- Launch-readiness Solana→Robinhood-Chain rewrite untouched, still no
  PR. Building blocks (#156, #160, #162) still merged and ready.
- Rienda M1-M5 last reported by Dennis 2026-07-31 — now 21 days stale.
- Kill-criteria: none tripped (`CLAUDE.md`, launch checklist).
- **Process.** The green-CI-but-unmerged recovery pattern (#142 …
  #174, 10 occurrences) has been superseded since #175 by a *new*
  failure mode: genuinely red CI on the `nexus` Vercel project caused
  by a plan/cron-frequency mismatch, unrelated to the docs-only diffs.
  This directly overlaps the still-unanswered auto-merge-for-planning-
  PRs ask (open since 2026-08-09, now 12+ days) — resolving that ask
  would also resolve tonight's backlog, since a merge policy that
  doesn't gate on the (irrelevant) `Vercel – nexus` check for
  `planning/**` + `STATUS.md`-only PRs would let these merge cleanly
  regardless of the cron/plan issue.
- **Production impact, separate from the deploy backlog.** The
  deposit-scan cron firing every 2 minutes but crediting 0 deposits
  per run (Solana RPC 429s) is a live product issue: agents sending
  USDC to the deposit address are very likely not getting credited
  right now. This is worth Dennis's attention independent of the
  Vercel plan question.

## 3. Tomorrow's plan (2026-08-22)

1. **Deposit-scan RPC rate-limiting — new, top priority.** Confirmed
   tonight via production runtime logs: `/api/v1/deposits/scan` is
   running on schedule but every Solana `getTransaction` call is
   returning HTTP 429, crediting 0 of ~10 transactions per run.
   Needs Dennis to look at `SOLANA_RPC_URL` — likely a shared/public
   RPC endpoint hitting rate limits; a paid or dedicated RPC provider
   (or backoff/batching in `apps/nexus/lib/deposits.ts`) is probably
   needed. This loop cannot query Solana RPC directly to confirm root
   cause beyond what the app's own logs show.
2. **`nexus` Vercel deploy blocker — now diagnosed, needs a decision.**
   Root cause confirmed: the `vdm-nexus` Vercel team's plan (Hobby-tier
   cron limit, best inference from the error text — not confirmed via
   a billing API) rejects the existing `*/2 * * * *` deposit-scan cron
   on any new deploy of the `nexus` project, blocking builds since
   2026-08-18T20:13Z. Two options: (a) upgrade the team to Pro, or (b)
   change the cron cadence in `apps/nexus/vercel.json` to something
   Hobby-compatible (trades off deposit-detection latency — see
   `CLAUDE.md`'s 2-minute cadence). Either needs Dennis's call; this
   loop won't change billing or the cron config unprompted.
3. **Auto-merge-for-planning-PRs ask — now directly relevant to #1/#2.**
   Open and unanswered since 2026-08-09 (12+ days). Resolving it (merge
   on green CI, or don't gate `planning/**`-only PRs on the `Vercel –
   nexus` check) would also clear the #175/#176/tonight backlog this
   new failure mode is creating. Repeating the ask, tied explicitly to
   tonight's finding.
4. **Launch readiness** (`marketing/token-launch-checklist.md`):
   Solana→Robinhood-Chain rewrite still unpicked, 24th night running.
   Building blocks merged and ready: #156, #160, #162.
5. **Rienda M1-M5** — status 21 days stale (last report 07-31).
   Continue the direct ask; carry forward unchanged.
6. **Health checks** — still egress-blocked, at least 22 consecutive
   confirmed nights. Needs Dennis's call on allowlisting or a
   broader-access job.
7. **Standing items** — #106 (89 days) and #95 (90 days) unchanged.
   Legal memo still status-tracking. May manual-submission backlog —
   one line, no more.

## Process watch

Tonight surfaced two concrete, previously-undiagnosed issues: (1) the
`nexus` Vercel project's builds have been blocked for 3 days by a
Hobby-plan cron-frequency limit, not a stalled pipeline as #176
guessed — confirmed via the Vercel API and the bot's own error text;
and (2) the deposit-scan cron that survived that block is running but
crediting 0 deposits per run due to Solana RPC 429s, a separate and
more urgent production issue. Both need Dennis's decision; this loop
has no further tools to fix either. #175 and #176 stay unmerged
tonight (genuinely red CI, not the earlier false-stuck pattern) and
tonight's own PR is likely to join them for the same reason.

full plan: planning/daily/2026-08-21.md
