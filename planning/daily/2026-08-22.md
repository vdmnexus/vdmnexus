# Daily review — 2026-08-22

## 0. Feedback from #nexus

Read `#nexus` (`C0BDPTS89QS`) back through 2026-08-17. All five messages
in that window are this loop's own automated summaries (08-17 through
08-21) — zero replies or new instructions from Dennis. Nothing to apply.
Flagging plainly: five straight nightly summaries with concrete decision
asks (Vercel plan vs. cron cadence, RPC provider, auto-merge, Rienda
status) have drawn no response at all.

No business-plan instructions — `business-plan.md` untouched, and still
does not exist in `vdmnexus/internal` (never created without an explicit
ask).

## 1-2. Gather + review

**PRs #175 (08-19), #176 (08-20), #177 (08-21) — still open, still
genuinely CI-red, unchanged tonight.** Re-verified independently rather
than trusting the PR bodies:

- `pull_request_read get_status` on #175 just now: `Vercel – nexus` =
  `failure` ("Deployment failed"), same as 08-19. The other three Vercel
  checks (docs/console/vdmnexus-web) and all 4 GitHub Actions checks are
  green on all three PRs. `mergeable_state: unstable` on all three —
  a failing required check, not a git conflict.
- Confirmed the root cause independently via the Vercel API: team
  `vdm-nexus` (`team_7ESg4uBvikjA7o4JqLGm8Asd`) is on the **Hobby**
  plan. `apps/nexus/vercel.json`'s deposit-scan cron (`*/2 * * * *`)
  exceeds the Hobby daily-cron limit, so any new build of `nexus`
  fails pre-build. None of the three PRs touch `vercel.json` (docs-only
  diffs), so the check will keep failing until Dennis either upgrades
  the plan or changes the cron cadence — this loop does not make that
  call or touch the config (out of scope: no deploys, no config
  changes).
- Production `nexus.vdmnexus.com` is unaffected by the stalled
  pipeline but stale — still serving the 2026-08-17 build, 5 days
  without a redeploy.
- **Independently confirmed the RPC-429 finding is still live right now**,
  not just a one-time observation: pulled `nexus` production runtime
  logs for the last 6h. Every `/api/v1/deposits/scan` run (still firing
  every ~2 min) logs `Error: RPC getTransaction HTTP 429` on the same
  three transaction signatures (`3LYWLZCh…`, `NRmJUXVBD…`, `3wajbBErd…`)
  every single run, `credited: 0` on every run. These three specific
  deposits cannot be credited under the current `SOLANA_RPC_URL` — this
  needs a rate-limit fix or a paid RPC endpoint, independent of the
  Vercel plan question.
- Not merging #175/#176/#177 tonight — same standing "never merge
  without confirmed-green CI" policy the last three nights held to.
  This session's own PR will hit the same check for the same reason;
  not merging it either. **Backlog is now 3 open planning PRs and
  about to become 4** — worse than any prior single-night stuck
  occurrence in this loop's history.
- No product code merged since #162 (2026-08-06) — 16 days.
- #106 (cards-v1 spec, merge-or-close decision) — 90 days open,
  unchanged. #95 (Polymarket agent, blocked on Spanish counsel) —
  91 days open, unchanged.

**Health checks (tracked object 3):** still not run — confirmed via
verbose curl against `nexus.vdmnexus.com/api/health` that this
session's egress proxy returns `403 Forbidden` on the CONNECT tunnel
itself (`Establish HTTP proxy tunnel to nexus.vdmnexus.com:443` →
`HTTP/1.1 403 Forbidden`), identical to every prior confirmed night
since 2026-07-31. 23rd consecutive confirmed night. Not an app-layer
failure — needs Dennis to either allowlist the three hosts for this
session's egress policy or move health checks to a job with broader
access.

**Rienda M1-M5:** last reported status unchanged (2026-07-31, from
Dennis) — spec complete; token + Uniswap v4 fee-burn hook contracts
built, 26 passing tests; M1 in development; M2-M5 not started. Now 22
days stale.

**Launch readiness (`marketing/token-launch-checklist.md`):** no PR has
picked up the Solana→Robinhood Chain operational rewrite. 24th
consecutive night as the top actionable item. No kill-criteria tripped
by anything found tonight.

**Legal memo:** still status-tracking only — email drafted, awaiting
Dennis send. No update.

## 3-4. Plan tomorrow (2026-08-23)

Tracked objects, priority order:

0. **Process integrity.** Auto-merge-for-planning-PRs ask: open since
   2026-08-09, now 13 days unanswered — repeating plainly, no further
   escalation in wording. Separately (not the same ask): the `nexus`
   Vercel deploy blocker and the deposit-scan RPC 429s both need a
   decision from Dennis and are now the most concrete, actionable items
   in this whole loop — surfaced at the top of tonight's Slack summary.
1. **Launch readiness:** Solana→Robinhood Chain rewrite, 25th night
   running with no PR.
2. **Rienda M1-M5:** repeat the direct ask for a fresh status update
   (07-31 report is now 22+ days stale).
3. **Health checks:** still egress-blocked, needs an allowlist decision
   or a relocated health-check job. 23rd confirmed night tonight.
4. **Standing blocked items:** #106 / #95 merge-or-close decisions,
   legal memo send status, May manual-submission backlog (one line).

## 5. Slack

Posted to `#nexus`. See message for the skimmable version; this file is
the full record.

full plan: `planning/daily/2026-08-22.md`
