# Daily review — 2026-09-04

## Step 0 — Slack feedback

- Read `#nexus` back through the last checkpoint (PR #190 / 2026-09-03
  22:15 CEST). No new messages since then — the message it posted is
  still the newest message in the channel. The channel's entire history
  remains this loop's own nightly summaries; zero human replies from
  Dennis anywhere in it. Nothing to apply.

## Gather

- `main` unchanged at `85bc1e7` (PR #174, merged 2026-08-19). No product
  PRs merged since #162 (2026-08-06) — 29 days.
- Planning-PR backlog unchanged and growing: #175 through #190 (sixteen
  PRs) are all still open, none merged. Tonight's PR makes seventeen.
- **Re-verified the `nexus` Vercel-check failure fresh, not carried
  forward.** `list_teams` confirms `vdm-nexus` is still on the Hobby
  plan. `list_deployments` for the `nexus` project shows no deployment
  since `dpl_87VBtuekp...` created 2026-08-19T20:13Z (the #174 merge) —
  18th consecutive night with zero new `nexus` builds. PR #190's own
  `get_status` still shows `Vercel – nexus: failure` ("Deployment
  failed"), the other three checks (`docs`, `console`, `vdmnexus-web`)
  green. Not merging #175-#190 or tonight's PR.
- **Re-confirmed the deposit-crediting failure directly against live
  production runtime logs** (last ~2h window via Vercel runtime logs,
  filtered on `deposits/scan`): 20 scan invocations in the window, every
  one completes with `"credited":0`; 70 log lines reference HTTP 429
  from the Solana RPC `getTransaction` calls. On-chain USDC deposits are
  very likely still not being credited to agent balances — 14 days
  running since first found 2026-08-21.
- Health checks: fresh `WebFetch` calls to `nexus.vdmnexus.com` and
  `verify.vdmnexus.com` both return `EGRESS_BLOCKED` from this session's
  outbound proxy — 36th consecutive confirmed night. Playground-inference
  check skipped, consistent with every prior night once the other two
  hosts fail.
- #106 (cards-v1 spec): still open, `mergeable_state: clean`, 103 days
  open (since 2026-05-24).
- #95 (Polymarket agent): still open, `mergeable_state: dirty` (merge
  conflict against `main`), 104 days open (since 2026-05-23).
- Rienda M1-M5: no fresh report since 2026-07-31 — 35 days stale.
- Launch-readiness checklist (`marketing/token-launch-checklist.md`):
  T-14/T-48h/T-0 steps still describe Solana tooling despite the
  2026-07-30 re-venue to Uniswap v4 / Robinhood Chain — 37 nights
  unpicked. No changes to the file or its kill-criteria since last read.

## Review

- No merges, no regressions, no kill-criteria tripped (checked both
  `CLAUDE.md` and the token-launch-checklist's own kill-criteria list).
- Three decisions remain outstanding in `#nexus`, still with zero human
  reply anywhere in the channel's history:
  1. Vercel plan-vs-cron-cadence for `nexus`, open since 2026-08-21
     (14 days) — upgrading `vdm-nexus` to Pro, or dropping the
     deposit-scan cron to once-daily, either unblocks the
     seventeen-PR backlog.
  2. auto-merge-for-planning-PRs, open since 2026-08-09 (26 days) —
     now the direct cause of the backlog's size.
  3. the deposit-crediting failure (financial stakes: on-chain USDC
     very likely not being credited), open since 2026-08-21 (14 days).
- Given the combination of a 26-day-old unanswered process ask and a
  14-day-old live financial-stakes bug, with zero human replies anywhere
  in `#nexus`'s history (including two prior direct-notification
  attempts, one of which a 2026-08-31 check found had not reached
  Dennis via DM), this session is again sending tonight's summary
  through the scheduled-task notification channel directly, in addition
  to the `#nexus` post.

## Plan for 2026-09-05

1. Keep repeating the three open asks verbatim, no further escalation in
   wording: (a) Vercel plan-vs-cron-cadence for `nexus`; (b) auto-merge
   for planning-only PRs; (c) the deposit-crediting RPC 429s.
2. Launch-readiness Solana→Robinhood Chain rewrite — still unpicked,
   38th night.
3. Rienda M1-M5 — still needs a fresh status report, 36 days stale.
4. Health checks — expect egress-blocked, 37th night.
5. If the Vercel check clears before the next session, merge the
   backlog in strict order (#175 → ... → #190 → tonight's PR) rather
   than starting fresh.

Standing, lower priority: #106 (merge-or-close, 103 days) and #95
(dirty, 104 days, blocked on Spanish counsel) unchanged. Legal memo
still status-tracking (drafted, awaiting Dennis's send). May
manual-submission backlog unchanged, one line only.

full plan: `planning/daily/2026-09-04.md`
