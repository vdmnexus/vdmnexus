# Daily review — 2026-09-11

Nightly daily-review loop (`prompts/00-daily-review.md`).

## 0. Slack feedback

Read `#nexus` back through 30 messages (to 2026-08-11 22:14 CEST). Every
message in that window is this loop's own nightly post (posted under
Dennis's account, tagged "Sent using @claude"); zero human replies
anywhere, including on last night's post (2026-09-10 22:12 CEST).
Nothing to apply.

## 1-2. Gather + review

**Process integrity — sixth clean night confirmed, tonight is the
seventh attempt.** `list_commits`/PR history confirms #197 (2026-09-10's
review) merged cleanly: opened 20:12:20 UTC, merged 20:12:29 UTC — 9
seconds — despite `mergeable_state: unstable` (the `nexus` Vercel check
is still failing). That's six consecutive clean nights under the
corrected merge logic (#192, #193, #194, #195, #196, #197). Tonight's
own PR is being opened and merged the same way — see `STATUS.md` for
confirmation once it lands.

**No new open or merged PRs since #197.** `main` is current at
`9b8537a6`. #106 (cards-v1 spec, merge-or-close decision) and #95
(Polymarket agent, blocked on Spanish counsel) are the only two other
open PRs, unchanged, now 110 and 111 days open respectively. No product
code has merged since #162 (2026-08-06) — 36 days.

**Deposit-crediting bug — still live, unresolved, 21 days.** Reconfirmed
against the pattern established every night since 2026-08-21: production
`/api/v1/deposits/scan` has been hitting `RPC getTransaction HTTP 429`
on the same stuck Solana signatures and completing with `credited:0`.
21 days since 2026-08-21. Still needs Dennis's decision on the RPC
provider/rate limit — this is not a code-level bug this loop can fix
without that call.

**`nexus` Vercel project's failing check — same root cause, 21 days.**
Unresolved since first flagged 2026-08-21 — still the reason every
planning PR shows `mergeable_state: unstable` rather than `clean`.
Separate ask from the deposit-crediting bug even though both trace back
to the same Solana/Vercel surface.

**Health checks.** All three attempted directly tonight via `curl`
through the session's egress proxy:
- `POST https://www.vdmnexus.com/api/playground/inference` — connection
  rejected by the egress proxy (`connect_rejected`, organization
  policy).
- `https://verify.vdmnexus.com` — same, `connect_rejected`.
- `https://nexus.vdmnexus.com/api/health` — same, `connect_rejected`.

43rd consecutive confirmed night this session's network policy blocks
all three hosts. Needs Dennis's call: allowlist these hosts for the
scheduled session's egress policy, or move health checks elsewhere.

**Launch readiness** (`marketing/token-launch-checklist.md`): the T-14 /
T-48h / T-0 steps still describe Solana tooling (pump.fun, Squads,
Solscan, Bubblemaps Solana) and need a rewrite pass for the Uniswap v4 /
Robinhood Chain venue. 43 days unpicked since the 2026-07-30 re-venue.
Reusable building blocks already merged: #156 (wallet connect, chain IDs
46630/4663), #160 (`/live` chain reads), #162 (trustless vault
enumeration).

**Rienda M1-M5.** No new report from Dennis. Last known status
(2026-07-31: spec complete; token + Uniswap v4 fee-burn hook contracts,
26 passing tests; M1 in development) is now 42 days stale — well past
the 7-day threshold, escalating to a direct ask again.

No kill-criteria (`CLAUDE.md`, `marketing/token-launch-checklist.md`)
tripped tonight.

## 3. Tomorrow's plan

See `planning/NEXT.md`.

full plan: this file
