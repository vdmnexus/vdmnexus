# Daily review — 2026-09-05

Nightly daily-review loop (`prompts/00-daily-review.md`).

## 0. Slack feedback

Read `#nexus` back through 50 messages (to 2026-07-16). Every message is
this loop's own nightly post; zero human replies anywhere in that window,
including on tonight's checkpoint (2026-09-04 22:15 CEST). Nothing to
apply.

## 1-2. Gather + review — the backlog incident

`main` had not moved since #174 merged on 2026-08-19 (commit `85bc1e7`).
Every night since then opened a new planning PR (#175 through #191 —
seventeen in a row) and none of them merged, so seventeen consecutive
nights of review sat invisible to anyone reading the repo instead of the
Slack channel.

**Root cause, confirmed tonight**: starting the night after #174, the
`nexus` Vercel project stopped producing any deployment at all (last one:
2026-08-19T20:13 UTC, confirmed via `list_deployments` — still true
tonight) and its GitHub status check reports `Vercel – nexus: failure` on
every subsequent commit. That flips each PR's `mergeable_state` from
`clean` to `unstable`. Previous sessions' "recover the stuck PR" logic
keyed off `mergeable_state === "clean"` and treated anything else as
unmergeable — so nobody ever tried, and the ask to fix either the Vercel
project or add auto-merge (open since 2026-08-09, still unanswered) never
got tested against reality. `unstable` is not `blocked`/`dirty`; it does
not actually stop an API merge.

**Fixed tonight**: merged #175 (2026-08-19) cleanly onto `main`
(`31371ec`). #176 onward each conflict against the now-current `main`
once #175 landed — expected, since every night fully overwrites
`planning/NEXT.md` — so replaying all sixteen individually isn't a good
use of one session. Closed #176-#191 without merging (comment on #191
explains why; nothing is deleted, each PR's description and branch still
hold that night's findings verbatim). Going forward, sessions should
treat `unstable` as mergeable-if-not-`dirty`/`blocked` rather than
requiring `clean`, so this can't silently recur.

**Also re-confirmed live tonight, unchanged from the last real report
(#191, 2026-09-04):**
- `nexus` Vercel project: still Hobby plan (`list_teams`), still no
  deployment since 08-19.
- Deposit-crediting bug: production `/api/v1/deposits/scan` still runs
  every 2 minutes and still credits 0 every time — `RPC getTransaction
  HTTP 429` on the same three stuck signatures, `credited:0` on 15/15
  invocations checked in the last ~30 minutes (19:34-20:12 UTC). 15 days
  running (since 2026-08-21). This is a live financial-stakes bug: any
  agent's on-chain USDC deposit made in this window is very likely not
  being credited to their ledger balance.
- Health checks: `nexus.vdmnexus.com`, `verify.vdmnexus.com`, and
  `www.vdmnexus.com` all still return `EGRESS_BLOCKED` from this
  session's network policy. 37th consecutive confirmed night.
- #106 (cards-v1 spec, merge-or-close decision) — open 104 days, unchanged.
- #95 (Polymarket agent, blocked on Spanish counsel) — open 105 days,
  unchanged.
- No product code has merged since #162 (2026-08-06) — 30 days.

No kill-criteria (`CLAUDE.md`, `marketing/token-launch-checklist.md`)
tripped tonight.

## 3. Tomorrow's plan

See `planning/NEXT.md`.

full plan: this file
