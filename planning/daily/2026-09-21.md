# Daily review — 2026-09-21

## 0. Slack feedback check

Read `#nexus` back through the last checkpoint (2026-09-20 22:13 CEST).
The only message in that window is this loop's own automated
2026-09-20 daily-review post — zero human replies. Nothing to apply.
(All messages in the channel's history continue to post under the
Dennis Slack identity because the integration authenticates as that
account — not evidence of a human reply.)

## 1-2. Gather + review

- **PRs/commits since last run:** none. `main` is still at the #207
  merge commit (`4dc6a4c8a832`, 2026-09-20T20:13:37Z). No new PRs
  opened or merged outside this loop's own cadence, no other commits
  to `main`.
- **#207 (2026-09-20 daily review)** — confirmed merged cleanly the
  same session despite `mergeable_state: unstable` (the `nexus`
  Vercel check is still failing). **Sixteenth consecutive clean
  night** under the corrected merge logic (#192-#207). The fix stays
  proven; no action needed here.
- **Open PRs, re-verified fresh:**
  - #106 (cards-v1 spec) — still open, unchanged, **120 days**. Still a
    merge-or-close decision for Dennis, not a build task.
  - #95 (Polymarket agent, Phase A) — still open, unchanged, **121
    days**. Still blocked on Spanish counsel.
- **Kill-criteria check** (`CLAUDE.md` + `marketing/token-launch-checklist.md`):
  nothing new trips a kill criterion tonight.

### Health checks (read-only)

All three hosts failed again tonight. `WebFetch` returned explicit
`EGRESS_BLOCKED` for both `nexus.vdmnexus.com/api/health` and
`verify.vdmnexus.com`; a direct `curl` POST to
`www.vdmnexus.com/api/playground/inference` hit a CONNECT-tunnel `403`
(`connect_rejected`, organization policy) — not an app-layer failure.
**53rd consecutive confirmed blocked night.** Unchanged root cause —
still needs a decision from Dennis: allowlist these hosts for the
scheduled session's egress policy, or move the checks to run from
somewhere else.

### Deposit-crediting bug — reconfirmed fresh (not carried forward)

Pulled directly against live production runtime logs tonight via the
Vercel MCP tools (`get_runtime_logs` on the `nexus` project, last 2h
window): 30 `/api/v1/deposits/scan` invocations, all HTTP 200 at the
route level, every one logging `deposit.scan_completed ...
credited:0` after hitting `RPC getTransaction HTTP 429` on the same
three stuck signatures (`3LYWLZ...`, `NRmJUX...`, `3wajbB...`).
**31 days running** (since 2026-08-21). Needs Dennis's decision on the
RPC provider/rate limit — not another nightly re-check.

### `nexus` Vercel project — reconfirmed fresh (not carried forward)

`list_teams` confirms `vdm-nexus` is still the only team (Hobby-plan
symptoms unchanged — the `*/2min` deposit-scan cron is the diagnosed
blocker). `list_deployments` for the `nexus` project shows no
deployment newer than the `target: production` build
`dpl_kMXumydsSvke6TjRGXWJZXbdqA3g` (2026-08-19T20:12:40Z) — the same
build id the fresh runtime-log pull above shows still serving traffic
tonight. **33 days.** Still very likely the same root cause blocking a
code fix for the deposit bug from ever shipping.

### Rienda M1-M5

No report from Dennis since 2026-07-31 (spec complete; token + Uniswap
v4 fee-burn hook contracts, 26 passing tests; M1 in development).
**52 days stale.**

### Launch readiness (`marketing/token-launch-checklist.md`)

No movement. The T-14 / T-48h / T-0 steps still describe Solana
tooling (pump.fun, Squads, Solscan, Bubblemaps Solana) and still need
a rewrite pass for the Uniswap v4 / Robinhood Chain venue — **53 days
unpicked** since the 2026-07-30 re-venue.

## 3-4. Plan tomorrow + persist

See `planning/NEXT.md` (overwritten alongside this file) for the full
ordered plan. Two asks stay open and unanswered in `#nexus`: the
`nexus` Vercel project's failing check (33 days) and auto-merge for
planning PRs (43 days).

`STATUS.md` updated with tonight's clean-merge confirmation for #207
and the fresh deposit-bug / Vercel-check re-verification.

full plan: `planning/daily/2026-09-21.md`
