# Daily review — 2026-09-17

## 0. Slack feedback check

Read `#nexus` back through the last checkpoint (2026-09-16 22:13 CEST).
The only message in that window is this loop's own automated
2026-09-16 daily-review post (sent under the Dennis Slack identity,
since the integration authenticates as that account) — zero human
replies. Nothing to apply.

## 1-2. Gather + review

- **PRs/commits since last run:** none. `main` is still at the #203
  merge commit (`aad33f42dfe9`, 2026-09-16T20:12:39Z). No new PRs
  opened or merged outside this loop's own cadence, no other commits
  to `main`.
- **#203 (2026-09-16 daily review)** — confirmed merged cleanly the
  same session despite `mergeable_state: unstable` (the `nexus` Vercel
  check is still failing). **Twelfth consecutive clean night** under
  the corrected merge logic (#192-#203). The fix stays proven; no
  action needed here.
- **Open PRs, re-verified fresh:**
  - #106 (cards-v1 spec) — still open, unchanged, **116 days**. Still a
    merge-or-close decision for Dennis, not a build task.
  - #95 (Polymarket agent, Phase A) — still open, unchanged, **117
    days**. Still blocked on Spanish counsel.
- **Kill-criteria check** (`CLAUDE.md` + `marketing/token-launch-checklist.md`):
  nothing new trips a kill criterion tonight. The live deposit-crediting
  bug below is a running concern, not a fresh trigger.

### Health checks (read-only)

All three hosts failed again tonight. `WebFetch` returned explicit
`EGRESS_BLOCKED` for `nexus.vdmnexus.com/api/health` and
`verify.vdmnexus.com`; a direct `curl` POST to
`www.vdmnexus.com/api/playground/inference` hit a `403` on the CONNECT
tunnel (`connect_rejected`, organization policy) — not an app-layer
failure. **49th consecutive confirmed blocked night.** Unchanged root
cause — still needs a decision from Dennis: allowlist these hosts for
the scheduled session's egress policy, or move the checks to run from
somewhere else.

### Deposit-crediting bug

Carried forward from the last direct confirmation (2026-09-16: 90
occurrences of `RPC getTransaction HTTP 429` across all 30
`deposit.scan_completed` events in a two-hour window, every one
`credited: 0`). **27 days running** (since 2026-08-21). Needs Dennis's
decision on the RPC provider/rate limit — not another nightly re-check.
Flagged for a fresh direct pull next session rather than carrying
forward again.

### `nexus` Vercel project

Carried forward from the last direct confirmation (2026-09-16:
`list_teams` showed `vdm-nexus` still on the Hobby plan;
`list_deployments` showed no `nexus` deployment newer than
2026-08-19T20:13Z). **29 days.** Still very likely the same root cause
blocking a code fix for the deposit bug from ever shipping. Flagged
for a fresh direct pull next session.

### Rienda M1-M5

No report from Dennis since 2026-07-31 (spec complete; token + Uniswap
v4 fee-burn hook contracts, 26 passing tests; M1 in development).
**48 days stale.**

### Launch readiness (`marketing/token-launch-checklist.md`)

No movement. The T-14 / T-48h / T-0 steps still describe Solana tooling
(pump.fun, Squads, Solscan, Bubblemaps Solana) and still need a rewrite
pass for the Uniswap v4 / Robinhood Chain venue — **49 days unpicked**
since the 2026-07-30 re-venue.

## 3-4. Plan tomorrow + persist

See `planning/NEXT.md` (overwritten alongside this file) for the full
ordered plan. Two asks stay open and unanswered in `#nexus`: the
`nexus` Vercel project's failing check (29 days) and auto-merge for
planning PRs (39 days).

`STATUS.md` updated with tonight's clean-merge confirmation for #203.

full plan: `planning/daily/2026-09-17.md`
