# Daily review — 2026-07-01

## Step 0 — #nexus feedback
No new messages in `#nexus` since the 2026-06-30 22:06 CEST daily-review
post — that post is still the most recent message in the channel (no
replies, no new instructions from Dennis). Nothing to apply.

## PRs reviewed
- No merges since #119 (2026-06-30, the previous daily-review commit
  itself). Zero code PRs merged in the last 24h — a quiet day.
- **#118** — homepage fixes (remove founder placeholder, unify $NEXUS,
  surface LiveProof) — still open. Resolved the open question from the
  last two daily reviews: `mergeable_state` on the PR is **`unstable`**,
  which is GitHub's signal for "mergeable, but a non-required check is
  failing." That confirms **`Vercel – wc`** is not a required check —
  every app-relevant deploy (web, nexus, verify, docs, console) is green,
  and the PR is safe to merge despite the red X. Recommending Dennis
  merge it now instead of re-checking a third day running.
- **#114** — WC dedicated site — still blocked on Dennis: Vercel project
  + DNS CNAME.
- **#106** — cards-v1 spec — still blocked on Dennis: merge or close
  decision.
- **#95** — Polymarket agent Phase A — still blocked on Spanish
  compliance counsel.

## Roadmap status
- Item 7 (compliance one-pager, `prompts/06-compliance-one-pager.md`)
  has now been "tomorrow's plan" for two days running (2026-06-30 and
  2026-07-01) without a session actually running it. Worth naming why:
  `prompts/00-daily-review.md` is read-only on code by its own rules —
  this loop can *recommend* item 7 but cannot *build* it. It needs a
  separate session invoked with `prompts/06-compliance-one-pager.md`
  directly (the file exists, is fully spec'd, 3.1KB, zero Dennis
  blockers). Flagging so the recommendation doesn't silently repeat a
  third time without anyone noticing it's stalled on invocation, not on
  content.
- Items 2, 3, 4, 8 (manual-submission backlog) and the CDP Bazaar
  bootstrap-call question — unchanged since 2026-06-30, still sitting
  on Dennis's side.
- No kill-criteria tripped.

## Tomorrow's plan: 2026-07-02
1. **Run `prompts/06-compliance-one-pager.md` directly** (roadmap item
   7) — not just list it in `NEXT.md`. This is the second day it's been
   the top recommendation; it needs its own session invocation, since
   the daily-review loop itself can't write code.
2. **Dennis: merge #118.** CI question is resolved — `Vercel – wc`
   failing is a non-required check, PR `mergeable_state` is `unstable`
   (mergeable). No further re-checks needed from this loop.
3. **Surface the standing manual-submission backlog once more, briefly**
   — five items (#2 awesome-x402 + x402.direct, #3 Sepolia mint, #4
   AgentKit PR, #8 SendAI plugin) plus the CDP Bazaar bootstrap-call
   check, all ~5-30 min Dennis-side tasks, unchanged since May/June.
4. No new outreach/distribution branches until the backlog clears.

full plan: planning/daily/2026-07-01.md
