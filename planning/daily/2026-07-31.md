# Daily review — 2026-07-31

## 0. Feedback from #nexus

No explicit instructions from Dennis since the 2026-07-30 post (checked
`#nexus` from 2026-07-30 22:09 CEST onward, and via a targeted search for
messages `from:dennis@vdmnexus.com after:2026-07-30` — zero results). Nothing
to apply. `business-plan.md` still does not exist in `vdmnexus/internal` —
not created without an explicit go-ahead (still hasn't been given).

## 1. Gather / Review

Since the last daily review (2026-07-30), three PRs merged:

- **#149** — Two-layer homepage story (Trust/live + Capital/Rienda-in-dev),
  `/disclosures` un-gated and live pre-launch, `/security` updated with a
  Rienda gap card, `/token` + `/whitepaper` re-venued from pump.fun/Solana to
  a Uniswap v4 $NEXUS/USDC pool with a fee-burn hook on Robinhood Chain,
  `/wc26` removed from the marketing site.
- **#150** — Root-caused the live playground `upstream_error`: the nexus
  deployment's Upstash Redis credential is dead, so every rate-limited route
  threw and returned an empty-body 500. Fixed in three layers (SDK parses
  non-JSON bodies defensively, nexus rate-limiter fails open with a loud log
  instead of crashing, the web playground route surfaces the real upstream
  error). Restoring the Upstash credential itself is still a manual Vercel
  dashboard action — not done by this PR. Also: post-#149 dead-link/nav audit,
  nav+footer IA for the two-layer story, `docs/VOICE.md` + `copy-lint.mjs`
  wired into CI.
- **#151** — Dedicated `/rienda` page (nav/footer now point at it instead of
  the homepage anchor); retargeted this nightly loop's tracked objects
  (`prompts/00-daily-review.md`) to launch-readiness / Rienda M1-M5 / site
  health / standing-blocked, in that order; fixed a dead OpenRouter fast-tier
  model slug (`meta-llama/llama-3-70b-instruct` → `llama-3.3-70b-instruct`).

**#114** (WC26 dedicated site) was **closed** today (not merged) — resolves
last night's "close #114?" decision request; #150 had already pulled the
wc26 pages off the marketing site, so the standalone app no longer had a
purpose. Dropping it from tracking.

**Launch-readiness checklist** (`marketing/token-launch-checklist.md`): of
the T-14 items, `/disclosures` and `/security` now read as live and
un-gated per #149/#150 — worth noting even though the checklist file itself
wasn't checkbox-updated (out of this loop's write scope; logging here
instead). The checklist's own flag stands unchanged: the T-14/T-48h/T-0
operational steps still describe Solana tooling (pump.fun deploy, Squads,
Solscan, Bubblemaps Solana) and need a rewrite pass for the Robinhood
Chain / Uniswap v4 venue before any launch date is set — none of today's
three PRs did that rewrite. Also worth flagging: several T-0/T-48h steps
(three separate Squads multisigs, Bubblemaps-Solana clustering checks) are
Solana-specific and may need to become Safe-multisig / Robinhood-explorer
equivalents rather than a like-for-like rewrite — a decision, not just a
copy pass.

No kill-criteria tripped (checked `CLAUDE.md` + the checklist's own list).

**Rienda M1-M5**: no new report from Dennis since the 2026-07-31 status
already on record (spec complete; token + Uniswap v4 fee-burn hook
contracts built, 26 passing tests; M1 vault + policy engine in
development; M2-M5 not started). Not stale — no update needed tonight.

**Site/API health checks — could not run.** All three read-only checks
(`POST /api/playground/inference`, `verify.vdmnexus.com`,
`nexus.vdmnexus.com/api/health`) failed at the network layer: this
session's outbound proxy returned `403` on the CONNECT to
`www.vdmnexus.com`, `verify.vdmnexus.com`, and `nexus.vdmnexus.com`
specifically — a gateway policy denial, not a response from the sites
themselves (confirmed via `$HTTPS_PROXY/__agentproxy/status`, which logged
all three as `connect_rejected`). This is a change from prior nights where
these checks apparently succeeded — either the environment's outbound
policy changed, or this is the first night the checks ran from a
network-restricted session. Flagging for Dennis: this loop cannot verify
site/API health until vdmnexus.com subdomains are reachable from this
session's egress policy, or the checks are moved to a job that has such
access.

## 2. Kill-criteria check

None tripped in `CLAUDE.md` or `marketing/token-launch-checklist.md`.

## 3. Tomorrow's plan — see `planning/NEXT.md`

## 4. Standing-blocked items

- **#106** (cards-v1 spec) — open since 2026-05-24, no activity. Still a
  merge-or-close decision, unchanged.
- **#95** (Polymarket agent, Phase A) — open since 2026-05-23, blocked on
  Spanish counsel. Unchanged.
- **Legal memo** — status-tracking only per last retarget: email drafted,
  awaiting Dennis to send. No change reported.
- May manual-submission backlog (discovery listings, Sepolia mint, AgentKit
  PR, SendAI plugin) — still open, Dennis-blocked, one line only.

full plan: planning/NEXT.md
