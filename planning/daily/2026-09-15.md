# Daily review — 2026-09-15

## 0. Slack feedback check

Read `#nexus` back through the last checkpoint (2026-09-14 22:12 CEST).
The only message in that window is this loop's own automated
2026-09-14 daily-review post under the Dennis Slack identity — zero
human replies. Nothing to apply.

## 1-2. Gather + review

- **PRs/commits since last run:** none. `main` is still at the #201
  merge commit (`97cc90e393f4`, 2026-09-14T20:12:05Z). No new PRs
  opened or merged outside this loop's own cadence, no other commits
  to `main`.
- **#201 (2026-09-14 daily review)** — confirmed merged cleanly the
  same session despite `mergeable_state: unstable` (the `nexus` Vercel
  check is still failing). **Tenth consecutive clean night** under the
  corrected merge logic (#192-#201). The fix stays proven; no action
  needed here.
- **Open PRs, re-verified fresh:**
  - #106 (cards-v1 spec) — still open, unchanged, **114 days**. Still a
    merge-or-close decision for Dennis, not a build task.
  - #95 (Polymarket agent, Phase A) — still open, unchanged, **115
    days**. Still blocked on Spanish counsel.
- **Kill-criteria check** (`CLAUDE.md` + `marketing/token-launch-checklist.md`):
  nothing new trips a kill criterion tonight. The live deposit-crediting
  bug below is a running concern, not a fresh trigger.

### Health checks (read-only)

All three hosts failed again tonight — `verify.vdmnexus.com`,
`nexus.vdmnexus.com/api/health`, and the `www.vdmnexus.com`
playground-inference POST all hit `connect_rejected` on the session's
own egress proxy CONNECT tunnel (organization policy), not an
app-layer failure. **47th consecutive confirmed blocked night.**
Unchanged root cause — still needs a decision from Dennis: allowlist
these hosts for the scheduled session's egress policy, or move the
checks to run from somewhere else.

### Deposit-crediting bug — carried forward, unresolved

Last confirmed directly against live production runtime logs on
2026-09-14: `RPC getTransaction HTTP 429` on every scanned signature,
every `deposit.scan_completed` event `credited: 0`. **25 days running**
(since 2026-08-21). This session did not re-pull runtime logs (no new
information expected overnight with no code deploy since 08-19) but
the bug has no reason to have self-resolved. Needs Dennis's decision on
the RPC provider/rate limit — not another nightly re-check.

### `nexus` Vercel project — carried forward, unresolved

Last confirmed on 2026-09-14: `vdm-nexus` still on the Hobby plan, no
`nexus` deployment newer than 2026-08-19T20:13Z. **27 days.** Still
very likely the same root cause blocking a code fix for the deposit
bug from ever shipping. Both process-integrity asks below remain open.

### Rienda M1-M5

No report from Dennis since 2026-07-31 (spec complete; token + Uniswap
v4 fee-burn hook contracts, 26 passing tests; M1 in development).
**46 days stale.**

### Launch readiness (`marketing/token-launch-checklist.md`)

No movement. The T-14 / T-48h / T-0 steps still describe Solana tooling
(pump.fun, Squads, Solscan, Bubblemaps Solana) and still need a rewrite
pass for the Uniswap v4 / Robinhood Chain venue — **47 days unpicked**
since the 2026-07-30 re-venue.

## 3-4. Plan tomorrow + persist

See `planning/NEXT.md` (overwritten alongside this file) for the full
ordered plan. Two asks stay open and unanswered in `#nexus`: the
`nexus` Vercel project's failing check (27 days) and auto-merge for
planning PRs (37 days).

`STATUS.md` updated with tonight's clean-merge confirmation for #201.

full plan: `planning/daily/2026-09-15.md`
