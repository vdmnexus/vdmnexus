# Daily review — 2026-09-01

## 0. Slack feedback applied

Read `#nexus` back through the last checkpoint (2026-08-31, 22:15:39
CEST, PR #187's own summary) and the full window before it back to
08-11. Every message in the channel is this loop's own automated
nightly summary (posted under Dennis's Slack identity but tagged
"Sent using @claude") — zero replies from a human anywhere in the
window. Nothing to apply from step 0.

## 1. Gather

- **Re-verified the `nexus` Vercel-check failure fresh, not carried
  forward.** `list_teams` confirms `vdm-nexus` is still on the Hobby
  plan. PR #187's combined status is still `Vercel – nexus: failure`
  ("Deployment failed"), unchanged since 2026-08-18T20:13Z — the
  **15th consecutive night**. The other three checks (`vdmnexus-web`,
  `docs`, `console`) are green. Not merging #175-#187 or this PR.
  Backlog is now **fourteen** deep (#175-#187, plus this one).
- **Re-confirmed the deposit-crediting failure directly against live
  production runtime logs** (last ~10 minutes, ~2-minute cadence):
  every run of `/api/v1/deposits/scan` still logs `RPC getTransaction
  HTTP 429` on the same three transaction signatures and completes
  with `txs_seen:10, credited:0, skipped:10`. On-chain USDC deposits
  are very likely still not being credited — 11 days running since
  first found (2026-08-21).
- No product code merged since #162 (2026-08-06) — 26 days. #106
  (cards-v1, 100 days, `mergeable_state: clean`) and #95 (Polymarket,
  101 days, `mergeable_state: dirty`) unchanged.
- Health checks: still egress-blocked. `WebFetch` against both
  `nexus.vdmnexus.com/api/health` and `verify.vdmnexus.com` returned
  explicit `EGRESS_BLOCKED` errors from this session's own network
  policy — 33rd consecutive confirmed night.
- `STATUS.md`, `marketing/token-launch-checklist.md`, `CLAUDE.md`
  kill-criteria: read; no changes, no kill-criteria tripped.
- `business-plan.md` still does not exist in `vdmnexus/internal` — not
  creating it without an explicit go-ahead from Dennis (step-0 rule).
- If the Vercel check clears before the next session, merge the
  backlog in order (#175 → ... → #187 → this PR) rather than starting
  fresh.

## 2. Review

- Fifteen consecutive nights of a diagnosed, unchanged platform
  failure (Hobby-plan cron-frequency limit rejecting
  `apps/nexus/vercel.json`'s `*/2 * * * *` deposit-scan cron) has now
  produced a fourteen-PR backlog of docs-only planning commits that
  cannot land on `main`. This is not new information, but it is now
  the single largest drag on this loop's own throughput.
- Separately, and with real financial stakes: the deposit-scan cron
  that *is* still running (on the last pre-block production deploy)
  is crediting zero deposits per run, every ~2 minutes, for the last
  11 days. Any agent that has sent USDC to the Nexus deposit address
  in that window very likely has not had its balance credited.
- Three decisions have sat unanswered in `#nexus` — a channel with
  zero human replies across its entire history — for 11, 11, and 23
  days respectively. Prior sessions (08-29 through 08-31) reported
  sending a "direct notification" alongside the Slack post given the
  stakes, and 08-31's session found the Slack DM channel itself empty,
  flagging a possible delivery gap. This session is escalating through
  the scheduled-task notification channel directly rather than relying
  on Slack alone, given 11+ days of unaddressed financial exposure.
- No kill-criteria tripped (`CLAUDE.md`, launch checklist).
- Launch-readiness Solana→Robinhood Chain rewrite: still unpicked,
  38th night. Rienda M1-M5: last report 07-31, now 32 days stale.

## 3. Tomorrow's plan (2026-09-02)

1. **Process integrity — Vercel plan-vs-cron-cadence for `nexus`.**
   Still the direct blocker on the fourteen-PR backlog. Needs Dennis
   to either upgrade `vdm-nexus` to Pro or drop
   `apps/nexus/vercel.json`'s deposit-scan cron to once-daily. Repeat
   plainly, no further escalation in wording beyond tonight's direct
   notification.
2. **Financial stakes — deposit-scan cron crediting 0 deposits per
   run.** Needs a look at `SOLANA_RPC_URL` capacity/rate limits,
   independent of the Vercel plan question. 11 days running.
3. **Auto-merge-for-planning-PRs ask.** Open since 2026-08-09, 23
   days, now directly responsible for the fourteen-PR backlog size.
4. **Launch readiness** (`marketing/token-launch-checklist.md`): the
   T-14/T-48h/T-0 rewrite for Uniswap v4 / Robinhood Chain still
   unpicked, 39th night. Building blocks merged and ready: #156,
   #160, #162.
5. **Rienda M1-M5** — 32 days stale (last report 07-31). Continue the
   direct ask; carry the same last-known status forward until one
   lands.
6. **Health checks** — still blocked, 34th consecutive confirmed
   night expected. Needs Dennis's call: allowlist `vdmnexus.com`,
   `verify.vdmnexus.com`, `nexus.vdmnexus.com` for this session's
   egress policy, or move health checks to a job with broader access.
7. **Standing items** — #106 (100 days, merge-or-close) and #95 (101
   days, blocked on Spanish counsel) unchanged. Legal memo still
   status-tracking. May manual-submission backlog — one line, no more.

full plan: planning/daily/2026-09-01.md
