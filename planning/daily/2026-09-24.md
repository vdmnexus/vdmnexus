# Daily review — 2026-09-24

## 0. Slack feedback check

Read `#nexus` back through the last checkpoint (2026-09-23 22:14 CEST).
The channel returned 50 messages, spanning 2026-08-04 through
2026-09-23 — every single one is this loop's own automated daily-review
post (confirmed by content, since all posts land under Dennis's own
Slack identity). Zero human replies anywhere in that span, extending
the previously-confirmed zero-reply window (which had only been
checked back to 2026-08-24) another three weeks earlier with no change.
Nothing to apply.

## 1-2. Gather + review

- **PRs/commits since last run:** none beyond this loop's own #210
  merge (`644ed3228b71`, 2026-09-23T20:13:46Z). No new PRs opened or
  merged outside this loop's cadence, no other commits to `main`.
- **#210 (2026-09-23 daily review)** — confirmed merged cleanly the
  same session despite `mergeable_state: unstable`. **Nineteenth
  consecutive clean night** under the corrected merge logic
  (#192-#210). No action needed here.
- **Open PRs, re-verified fresh:**
  - #106 (cards-v1 spec) — still open, unchanged, **123 days**. Still a
    merge-or-close decision for Dennis, not a build task.
  - #95 (Polymarket agent, Phase A) — still open, unchanged, **124
    days**. Still blocked on Spanish counsel.
- **Kill-criteria check** (`CLAUDE.md` + `marketing/token-launch-checklist.md`):
  nothing new trips a kill criterion tonight.

### Health checks (read-only)

All three hosts failed again tonight. `WebFetch` returned explicit
`EGRESS_BLOCKED` for both `nexus.vdmnexus.com/api/health` and
`verify.vdmnexus.com`; a direct `curl` POST to
`www.vdmnexus.com/api/playground/inference` hit a CONNECT-tunnel
`connect_rejected` (organization policy) — not an app-layer failure.
**56th consecutive confirmed blocked night.** Unchanged root cause —
still needs a decision from Dennis: allowlist these hosts for the
scheduled session's egress policy, or move the checks to run from
somewhere else.

### Deposit-crediting bug — reconfirmed fresh (not carried forward)

Pulled directly against live production runtime logs tonight via the
Vercel MCP tools (`get_runtime_logs` on the `nexus` project, last 30
minutes): 14 `/api/v1/deposits/scan` invocations, all HTTP 200 at the
route level, every one logging `deposit.scan_completed ...
credited:0` after hitting `RPC getTransaction HTTP 429` on the same
three stuck signatures (`3LYWLZ...`, `NRmJUX...`, `3wajbB...`).
**34 days running** (since 2026-08-21). Needs Dennis's decision on the
RPC provider/rate limit — not another nightly re-check.

### `nexus` Vercel project — reconfirmed fresh (not carried forward)

`list_teams` confirms `vdm-nexus` is still the only team. `list_deployments`
for the `nexus` project (target: production) shows no deployment newer
than `dpl_kMXumydsSvke6TjRGXWJZXbdqA3g` (2026-08-19T20:12:40Z) — the
same build id the fresh runtime-log pull above shows still serving
traffic tonight. **36 days.** Still very likely the same root cause
blocking a code fix for the deposit bug from ever shipping.

### Rienda M1-M5

No report from Dennis since 2026-07-31 (spec complete; token + Uniswap
v4 fee-burn hook contracts, 26 passing tests; M1 in development).
**55 days stale.**

### Launch readiness (`marketing/token-launch-checklist.md`)

No movement. The T-14 / T-48h / T-0 steps still describe Solana
tooling (pump.fun, Squads, Solscan, Bubblemaps Solana) and still need
a rewrite pass for the Uniswap v4 / Robinhood Chain venue — **56 days
unpicked** since the 2026-07-30 re-venue.

## 3-4. Plan tomorrow + persist

See `planning/NEXT.md` (overwritten alongside this file) for the full
ordered plan. Two asks stay open and unanswered in `#nexus`: the
`nexus` Vercel project's failing check (36 days) and auto-merge for
planning PRs (46 days).

`STATUS.md` updated with tonight's clean-merge confirmation for #210
and the fresh deposit-bug / Vercel-check re-verification.

full plan: `planning/daily/2026-09-24.md`
